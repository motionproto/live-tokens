---
type: regex
target: last_message
pattern: '^\s*10:\s*table\b'
flags: 'mi'
---

Requirement 10 expects `10: table -` in the ANSWERS block. Records the reader scans and compares take `table`. One `card` per item is wrong here.
