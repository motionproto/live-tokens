// Wave 1 stub: `sync:skill-sources` (Wave 2) replaces this with a re-export of
// the generated module built from `.claude/skills`.
export const skillDocs: Record<string, Record<string, string[]>> = {};
export const SKILL_DOC = 'SKILL.md';
